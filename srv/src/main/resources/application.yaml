---
spring:
  profiles: default
  datasource:
    url: "jdbc:sqlite:/Users/i519518/cap-business-service/sqlite.db"
    driver-class-name: org.sqlite.JDBC
    initialization-mode: always
    hikari:
      maximum-pool-size: 1
